/**
 * Google Form — replace YOUR_FORM_ID with your embed ID from Google Forms.
 *
 * 1. Create a form at https://forms.google.com
 * 2. Click Send → Embed HTML → copy the URL inside src="..."
 * 3. Paste the form ID (the long string between /d/e/ and /viewform) below
 * 4. In form Settings → Responses, turn on email notifications for your inbox
 */
const GOOGLE_FORM_ID = 'YOUR_FORM_ID';

const GOOGLE_FORM_EMBED_URL = `https://docs.google.com/forms/d/e/${GOOGLE_FORM_ID}/viewform?embedded=true`;
const GOOGLE_FORM_DIRECT_URL = `https://docs.google.com/forms/d/e/${GOOGLE_FORM_ID}/viewform`;
